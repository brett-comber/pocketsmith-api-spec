get:
  operationId: getDataFeedsConnection
  summary: Retrieve the specified data feeds connection (bank feed)
  tags:
    - Data Feeds Connections
  parameters:
    - name: id
      in: path
      required: true
      description: The unique identifier of the data feeds connection.
      schema:
        type: integer
      example: 42
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/DataFeedsConnection'
    '403':
      $ref: '#/components/responses/403'
    '404':
      $ref: '#/components/responses/404'

put:
  operationId: updateDataFeedsConnection
  summary: Update the data feeds connection, potentially to submit a form
  tags:
    - Data Feeds Connections
  parameters:
    - name: id
      in: path
      required: true
      description: The unique identifier of the data feeds connection.
      schema:
        type: integer
      example: 42
  requestBody:
    content:
      application/json:
        schema:
          type: object
          properties:
            automatic_syncing:
              $ref: '#/components/schemas/DataFeedsConnection/properties/automatic_syncing'
            categorisation:
              $ref: '#/components/schemas/DataFeedsConnection/properties/categorisation'
            name:
              $ref: '#/components/schemas/DataFeedsConnection/properties/name'
            pending_transactions:
              $ref: '#/components/schemas/DataFeedsConnection/properties/pending_transactions'
            login_form:
              $ref: '#/components/schemas/DataFeedsConnection/properties/login_form'
            interactive_login_form:
              $ref: '#/components/schemas/DataFeedsConnection/properties/interactive_login_form'
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/DataFeedsConnection'
    '403':
      $ref: '#/components/responses/403'
    '404':
      $ref: '#/components/responses/404'
    '422':
      $ref: '#/components/responses/422'
